{
  "name": "senseshifter-refreshed",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "clean": "turbo run clean",
    "build": "turbo run build",
    "dev": "turbo run dev",
    "check-types": "turbo check-types",
    "tauri": "pnpm --filter tauri-app tauri"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}